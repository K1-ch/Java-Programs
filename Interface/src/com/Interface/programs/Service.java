package com.Interface.programs;
//100% abstraction & used when we don't know anything abt implementation
public interface Service {
	void count();// every interface method is always
	// public and abstract whether we r declairing or nt
}
